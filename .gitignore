# Firebase CLI's local deploy bookkeeping
.firebase/
firebase-debug.log

# Server job: installed packages, and the engine copies made before each deploy
functions/node_modules/
functions/shared/

# Downloads the tests keep between runs (Sleeper's and ESPN's public player lists)
tests/.cache/
