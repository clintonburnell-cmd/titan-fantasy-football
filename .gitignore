# Firebase CLI's local deploy bookkeeping
.firebase/
firebase-debug.log

# Server job: installed packages, and the engine copies made before each deploy
functions/node_modules/
functions/shared/

# Downloads the tests keep between runs (Sleeper's and ESPN's public player lists)
tests/.cache/

# The owner's private resume notes (status and to-dos); firebase.json keeps it off the website too
RESUME.md

# The Chrome extension's packages (its dist/ and engine.js copy are committed so the folder loads unpacked as is)
extension/node_modules/
